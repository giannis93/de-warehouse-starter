# ---- PostgreSQL Core Settings ----
POSTGRES_USER=postgres        # Username for the Postgres superuser (created at first container run)
POSTGRES_PASSWORD=postgres    # Password for POSTGRES_USER (set at initialization)
POSTGRES_DB=app_db            # Name of the default database created on startup
POSTGRES_PORT=5432            # Port Postgres listens to inside container (default 5432)
POSTGRES_HOST=localhost       # Hostname used when connecting locally (pytest from your laptop)

# ---- Smoke Test Parameters ----
SMOKE_TABLE=public.sample     # Fully-qualified table (schema.table) to count rows from
SMOKE_MIN_ROWS=10             # Minimum number of rows expected after running make load-sample

# ---- CI Override Example ----
# In CI with Docker services: set POSTGRES_HOST=postgres (service name in docker-compose)  
# Example: replace localhost with 'postgres' when tests run inside GitHub Actions runner

# Optional aliases: set these if your env uses different names; the test will read them too.
# POSTGRES_HOST mirrors PGHOST when your Compose/service uses POSTGRES_* naming.
# POSTGRES_HOST=localhost
# POSTGRES_PORT mirrors PGPORT.
# POSTGRES_PORT=5432
# POSTGRES_USER mirrors PGUSER.
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD mirrors PGPASSWORD.
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB mirrors PGDATABASE.
# POSTGRES_DB=app_db
# DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME are generic aliases many projects use.
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=app_db
